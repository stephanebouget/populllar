(function () {
    'use strict';

    angular.module('populllarApp')
        .constant('CONSTANTS', {
            'STRING_PER_PAGE': 'per_page',
            'STRING_USERNAME': 'username'
        });
})();